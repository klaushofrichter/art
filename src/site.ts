/** Where this gallery lives. Used for links that leave the page — an email
 *  body cannot carry a relative URL. */
export const SITE_URL = 'https://art.klaushofrichter.net';

/** Where the running build came from. Shown in the About room. */
export const REPO_URL = 'https://github.com/klaushofrichter/art';

/** How long an enquiry is remembered, matching the hold the email asks for. */
export const ENQUIRY_HOURS = 48;
